---
import { Sparkles, ArrowRight, Download } from "@lucide/astro";
---

<section
  class="py-8 md:py-12 px-6 md:px-12 bg-gradient-to-br from-green-100 via-blue-50 to-white relative overflow-hidden"
>
  <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 items-center">
    <div>
      <div class="flex items-center gap-2 mb-2">
        <span
          class="bg-gradient-to-r from-green-600 to-blue-600 text-white text-sm font-medium px-3 py-1 rounded-full animate-pulse"
        >
          <Sparkles class="w-4 h-4 inline mr-1" /> New
        </span>
      </div>
      <h1
        class="text-4xl md:text-5xl font-bold leading-tight mb-4 text-green-700"
      >
        WEBETU: Mobile Student Portal for Algerian Universities
      </h1>
      <p class="text-lg text-gray-700 mb-8">
        A comprehensive mobile application designed to enhance the student
        experience in Algerian universities.
      </p>
      <div class="flex flex-wrap gap-4">
        <a
          href="#features"
          class="px-6 py-3 bg-gradient-to-r from-green-600 to-blue-600 text-white font-medium rounded-lg hover:shadow-lg transition-all duration-300 transform hover:-translate-y-1 flex items-center gap-2"
        >
          Explore Features <ArrowRight class="w-4 h-4" />
        </a>
        <a
          href="#"
          class="px-6 py-3 border border-green-500 text-green-600 font-medium rounded-lg hover:bg-green-50 transition-all duration-300 transform hover:-translate-y-1 flex items-center gap-2"
        >
          Download App <Download class="w-4 h-4" />
        </a>
      </div>
    </div>
    <div class="relative">
      <div
        class="relative z-10 transform hover:rotate-0 hover:scale-102 transition-all duration-500 rounded-2xl overflow-hidden backdrop-blur-sm bg-white/80 border border-white/20 shadow-[0_8px_32px_rgba(0,0,0,0.12)]"
      >
        <div
          class="bg-gradient-to-r from-green-600 to-blue-600 text-white py-3 px-4 text-sm font-medium flex items-center justify-between backdrop-blur-sm bg-opacity-90"
        >
          <span class="flex items-center gap-6">
            <div class="flex space-x-1.5">
              <div class="w-3 h-3 rounded-full bg-red-400"></div>
              <div class="w-3 h-3 rounded-full bg-yellow-400"></div>
              <div class="w-3 h-3 rounded-full bg-green-400"></div>
            </div>
            Student Portal
          </span>
          <span
            class="text-xs bg-white/20 backdrop-blur-sm text-white px-3 py-1 rounded-full"
            >v1.0</span
          >
        </div>
        <div class="bg-white py-4 flex items-center"> 
          <div class="flex-1 flex flex-col items-center">
            <img
              src="/images/app-login.png"
              alt="WEBETU App Login Screen"
              class="w-full max-w-[100%] h-auto rounded-lg shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-[1.02]" 
            />
            <p class="text-center mt-4 text-sm text-gray-600 font-medium">
              Login Screen
            </p>
          </div>
          <div class="flex-1 flex flex-col items-center">
            <img
              src="/images/app-dashboard.png"
              alt="WEBETU App Dashboard"
              class="w-full max-w-[100%] h-auto rounded-lg shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-[1.02]" 
            />
            <p class="text-center mt-4 text-sm text-gray-600 font-medium">
              Dashboard
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  section::before {
    content: "";
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(
      circle,
      rgba(74, 222, 128, 0.1) 0%,
      rgba(96, 165, 250, 0.05) 50%,
      transparent 100%
    );
    /* Remove the animation property */
    z-index: 0;
  }

  /* You can keep or remove these keyframes as they won't be used anymore */
  @keyframes rotate {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  img {
    max-width: 80%;  <!-- Changed from 50% to 60% -->
    height: auto;
  }

  @keyframes rotate {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .relative {
    z-index: 1;
  }
</style>
